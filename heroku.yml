build:
  docker:
    web: Dockerfile
    worker:
      dockerfile: Dockerfile
      target: builder
    command: "RUN printenv"
release:
  image: worker
  command:
    - ./migrate.sh
    - "RUN printenv"